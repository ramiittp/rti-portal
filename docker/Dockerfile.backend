# /rti-portal/docker/Dockerfile.backend
FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache dumb-init
COPY backend/package*.json ./
RUN npm ci --only=production
COPY backend/ .
RUN mkdir -p uploads logs
EXPOSE 5000
USER node
CMD ["dumb-init","node","src/server.js"]